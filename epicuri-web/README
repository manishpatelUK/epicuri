install on linux requires

# this one was only needed for kdb, no longer required
# sudo apt-get install gcc-multilib

sudo apt-get install maven
sudo apt-get install git
sudo apt-get install rlwrap

sudo add-apt-repository ppa:webupd8team/java
sudo apt-get update
sudo apt-get install oracle-java8-installer
sudo apt-get install oracle-java8-set-default

 mvn jfx:native -DskipTests
to build management app


FOR ENCRYPTING PASSWORDS:
java -cp  ~/.m2/repository/org/jasypt/jasypt/1.9.2/jasypt-1.9.2.jar org.jasypt.intf.cli.JasyptPBEStringEncryptionCLI input="<PASSWORD TO ENCRYPT>" password=<THE ADMIN PASSWORD WE USUALLY USE> algorithm=PBEWITHMD5AndDES
