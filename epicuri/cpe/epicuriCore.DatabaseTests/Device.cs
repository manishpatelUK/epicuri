using System;
using System.Text;
using System.Collections.Generic;
using System.Linq;
using Microsoft.VisualStudio.TestTools.UnitTesting;

namespace epicuriCore.DatabaseTests
{
    [TestClass]
    public class Device
    {
        [TestMethod]
        public void CreateDevice()
        {/*
            epicuriCore.DatabaseModel.epicuriContainer db = new epicuriCore.DatabaseModel.epicuriContainer();

            epicuriCore.DatabaseModel.Device d = new DatabaseModel.Device
            {
                RestaurantId = 3,
                Hash = "testDevice",
                DeviceId = "000"
            };

            db.AddToDevices(d);
            db.SaveChanges();

            Assert.IsNotNull(d.Id);
          * */
        }
    }
}
